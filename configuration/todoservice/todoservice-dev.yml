spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5000/todolist
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:5002/eureka}
